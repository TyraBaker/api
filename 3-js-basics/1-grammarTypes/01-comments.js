//this is a javascript comment (use two forward-slashes or the shortcut)
// "ctrl" and "`" (left of one, called back-tick) opens a terminal.

console.log("Hello, JS World!");
    // Must use quotes within parentheses.
    //use button "f8" at top to load output.
    //runs program for us.

console.log("multi-line comments");
    /*Anything you want to be said
    can fall between manylines. 
    And until you end with an astrisk
    and a forward-slash,
    everything will be a comment.*/